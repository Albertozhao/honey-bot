### 🧪 This is a test for using Workers with Honeycomb.io.
### 👀 Using a Discord Bot command, the Worker runs on a webhook that hits the Discord API and responds with the image that the API fetches.
